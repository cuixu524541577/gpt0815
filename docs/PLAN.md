# 开发优化计划（分阶段，按步执行）

> 配套分析：docs/ANALYSIS.md。每阶段有明确目标、任务清单、验证方式。
> 全部阶段完成 = 完整、健壮、可部署的成品。

## 阶段 P0：基线固化（已基本完成，收尾中）
- [x] 前端 1:1 移植（22 JS + i18n + 模板）
- [x] 认证系统（密码/PBKDF2/限速/掩码）
- [x] compat 兼容层（90+ 端点对齐线上基准）
- [x] turb 核心融入（多邮箱源/驱动/服务）
- [x] 自动化任务真实执行（4/5 类型）
- [x] UPI 提链接入（配置门控）
- [x] 部署自检 smoke_test
- [ ] 端口冲突文档化（已完成）→ 部署模板补 5001 说明

## 阶段 P1：健壮性加固（下一步执行）
**目标：异常不崩溃、数据不丢失、配置可回滚**

| # | 任务 | 验证 |
|---|---|---|
| 1.1 | 配置写盘前自动备份 `config/*.py.bak`（保留 5 份） | 改配置后 .bak 存在且可恢复 |
| 1.2 | 数据目录每日自动备份（data/ → backups/，保留 7 天） | 模拟删除后恢复 |
| 1.3 | 日志轮转（注册日志/补跑日志按 5MB 轮转） | 造大日志验证轮转 |
| 1.4 | 任务异常告警：注册/补跑连续失败 N 次写 alert 文件 | 触发失败观察告警 |
| 1.5 | 全局异常兜底：所有 API 路由统一 try/except → JSON 500 | 触发异常返回 JSON 而非 HTML |
| 1.6 | 启动自检：data 目录/凭据/依赖完整性检查 | 缺依赖时启动失败并给出修复指引 |

## 阶段 P2：体验优化
**目标：好用、看得懂、不打扰**

| # | 任务 | 验证 |
|---|---|---|
| 2.1 | 页面不可见时暂停轮询（visibilitychange 已部分有，补全所有页） | 切后台看网络请求停止 |
| 2.2 | 系统指标"不可用"态优化（本地开发环境友好显示） | 本地显示提示文案 |
| 2.3 | 任务失败/成功 Toast 更明确（含邮箱主体） | 任务结束有反馈 |
| 2.4 | 导出/导入结果摘要更友好（成功/跳过/失败明细） | 导入脏数据看提示 |
| 2.5 | 大列表虚拟滚动或分页优化（万级账号） | 万级数据滚动流畅 |

## 阶段 P3：能力扩展
**目标：注册链路多条腿走路**

| # | 任务 | 验证 |
|---|---|---|
| 3.1 | 免接码注册路径（Agent Identity，参考 hyhang915） | 配置开关后注册跳过 SMS |
| 3.2 | 浏览器驱动注册（RoxyBrowser/CloakBrowser，turb 已带模块） | 配置 DRIVER 后走浏览器链路 |
| 3.3 | 通知集成（Telegram Bot 发任务结果） | 配置 token 后收到消息 |
| 3.4 | access_token_relogin 真实实现（免费计划刷新） | 账号 token 刷新成功 |
| 3.5 | 提链任务结果自动入库（付款链接 → 账号 extra） | 提链成功落库 |

## 阶段 P4：安全收官
**目标：上线前安全清单清零**

| # | 任务 | 验证 |
|---|---|---|
| 4.1 | 账号数据导出净化（CSV 公式注入：`=`/`+`/`-` 前缀转义） | 导出含 = 开头字段验证转义 |
| 4.2 | 上传/导入大小限制（防 DoS） | 超大文件被拒 |
| 4.3 | 请求体大小限制（Flask MAX_CONTENT_LENGTH） | 大 body 413 |
| 4.4 | 凭据文件权限收紧（data/ 600） | ls 验证 |
| 4.5 | 登录审计日志（成功/失败 + IP） | 登录记录可查 |
| 4.6 | 全端点安全回归（smoke_test 扩展） | 全绿 |

## 阶段 P5：发布交付
**目标：一键部署、开箱即用**

| # | 任务 | 验证 |
|---|---|---|
| 5.1 | docker-compose 一键部署（含 volume/健康检查） | compose up 可用 |
| 5.2 | 部署手册（docs/DEPLOY.md）：初始化/升级/备份/恢复 | 按手册走通 |
| 5.3 | 移除测试凭据说明 + 发布检查清单 | 清单勾完 |
| 5.4 | 版本号/CHANGELOG 管理 | git tag 规范 |

## 执行方式
- 每阶段独立提交，完成后跑 `python3 tests/smoke_test.py` 回归
- 每阶段结束给用户汇报：做了什么、验证结果、下一步
- 遇到线上 0.1.48 可参考的行为差异，先抓基准再实现（沿用 ground truth 方法）
